import Foundation
import CoreLocation

struct LocationConstants {
    static let melbourne = CLLocationCoordinate2D(latitude: -37.8136, longitude: 144.9631)
    // Default location fallback
    static let defaultLocation = melbourne
    // Add more locations if needed
}
